# ビルド成果物
dist/
bin/
obj/

# 配布用ZIP。
# 過去バージョン（0.5.x）は内部保有の方針なので、リポジトリには含めない。
# 公開する ZIP は GitHub Releases の添付ファイルとして上げること。
release/

# Visual Studio のユーザー個別設定
.vs/
*.user
*.suo

# 実行時に生成される設定（資格情報は含まないが、環境固有のDDC名が入る）
sites.json

# 経営判断・署名の検討経緯など、公開しない内部メモ
private/
